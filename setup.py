#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='Audio_Deepfake_Detector',
      version='0.0.1',
      description='A toolbox for detecting audio DeepFakes',
      packages=['src'],
      )