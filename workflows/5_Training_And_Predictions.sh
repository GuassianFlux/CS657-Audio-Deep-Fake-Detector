#!/bin/bash

# Workflow 5 - Making Predictions w/ Data Split: Loads data, creates and trains model, makes predictions with the initial data split
echo "=== Workflow 5 - Making Predictions w/ Data Split: Loads data, creates and trains model, makes predictions with the initial data split ==="

# Load saved model
python /workspaces/src/__main__.py